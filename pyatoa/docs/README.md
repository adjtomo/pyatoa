To build these docs, please run the following command, which will execute
all code within Jupyter notebooks (*.ipynb), generate intermediate .rst files
and finally the required html files for the docs page:

```bash
python convert.py
make html
```
